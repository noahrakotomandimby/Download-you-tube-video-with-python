Step 1: Install Python
Download Python from python.org.

IMPORTANT: During installation, make sure to check the box "Add Python to PATH".

Step 2: Install the library
Open your terminal (CMD on Windows or Terminal on Mac) and type the following command:

Bash
pip install yt-dlp

Step 3: Usage
Run the script with the command: python telechargeur.py.
or run directly the file

Paste the link of your video.

Choose the format.

Check your Desktop, a folder "Mes_Telechargements" will appear with your file inside.
